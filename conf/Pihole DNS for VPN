`vi /etc/dnsmasq.d/01-pihole.conf`

comment out `interface=eth0`

---

it might also work to set DNSMASQ_LISTENING=all in `/etc/pihole/setupVars.conf`.
Yet to be confirmed within next pihole update

---

additionally, it's possible to add multiple configs to `/etc/dnsmasq.d`
